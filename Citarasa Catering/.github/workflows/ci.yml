name: CI - Citarasa Catering

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-and-build:
    name: Typecheck, Test, and Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: "Citarasa Catering/package-lock.json"

      - name: Install dependencies
        working-directory: "Citarasa Catering"
        run: npm ci

      - name: Generate Prisma Client
        working-directory: "Citarasa Catering"
        run: npm run db:generate

      - name: Check TypeScript types
        working-directory: "Citarasa Catering"
        run: npm run typecheck

      - name: Run unit tests
        working-directory: "Citarasa Catering"
        run: npm test

      - name: Build Next.js application
        working-directory: "Citarasa Catering"
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/citarasa?schema=public"
          SESSION_SECRET: "ci-dummy-session-secret-for-build-step-minimal-32-chars"
        run: npm run build

